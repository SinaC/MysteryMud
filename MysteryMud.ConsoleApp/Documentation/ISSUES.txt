in CharacterEffects
    EffectsByTag can only store the last effect by tag and we havea rule to skip stacking if the source is not the same
working scenario:
    player A adds an effect with tag T -> stores effect1 in EffectsByTag[T] = effect1 and adds a new entry in Effects
    player B adds an effect with tag T -> stores effect2 in EffectsByTag[T] = effect2 and adds a new entry in Effects
    player B adds an effect with tag T -> effect is stacked because source is the same in EffectsByTag[T]
failed scenario:
    player A adds an effect with tag T -> stores effect1 in EffectsByTag[T] = effect1 and adds a new entry in Effects
    player B adds an effect with tag T -> stores effect2 in EffectsByTag[T] = effect2 and adds a new entry in Effects
    player A adds an effect with tag T -> stores effect3 in EffectsByTag[T] = effect3 and adds a new entry in Effects but it should have been stacked in effect1

unwanted behavior
dot: tick rate 2 and duration 5
tick evolution
id      factory         tick            dot
1       creation        remaining--
2                       remaining--
3                       remaining--     damage
4                       remaining--
5                       expored         damage not done because expired